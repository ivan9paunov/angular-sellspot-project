export interface User {
    username: string;
    email: string;
    password: string;
    accessToken: string;
    _id: string;
};

export interface UserData {
    username: string;
    email: string;
}